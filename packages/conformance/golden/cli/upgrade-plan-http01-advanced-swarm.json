{
  "name": "upgrade-plan-http01-advanced-swarm",
  "argv": [
    "upgrade",
    "-n",
    "--to",
    "0.29.1"
  ],
  "env": {
    "PSTACK_DATA": "/tmp/pstack-golden-data",
    "PSTACK_INSTALL_DIR": "/usr/local/bin"
  },
  "cwd": "packages/pstack",
  "code": 0,
  "stdout": "pstack <VERSION> → 0.29.1   (preview.example.com, http01, advanced UI, swarm)\n  → install pstack 0.29.1\n  [dry-run] install pstack 0.29.1\n  → rebuild and re-init as the new version\n  [dry-run] rebuild and re-init as the new version\n  then, as the newly installed version:\n    pstack build-image\n    pstack build-image --ui\n    pstack init --domain 'preview.example.com' --acme-email 'ops@example.com' --challenge http01 --ui advanced --orchestrator swarm (with the existing PSTACK_TOKEN)\n\n  Dry run — nothing was installed, built or recreated.\n",
  "stderr": ""
}
