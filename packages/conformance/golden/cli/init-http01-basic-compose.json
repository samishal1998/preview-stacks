{
  "name": "init-http01-basic-compose",
  "argv": [
    "init",
    "--domain",
    "preview.example.com",
    "--acme-email",
    "ops@example.com",
    "--challenge",
    "http01",
    "--ui",
    "basic",
    "--orchestrator",
    "compose"
  ],
  "env": {
    "PSTACK_DATA": "/tmp/pstack-golden-data",
    "PSTACK_TOKEN": "golden-token-0123456789abcdef0123456789abcdef"
  },
  "cwd": "packages/pstack",
  "code": 0,
  "stdout": "\ncontrol stack up  (project pstack-control)\n  config    <DATA>/control/docker-compose.yml\n  registry  <DATA>/deployments\n  networks  preview-ingress, preview-shared   (bridge; declare both as `external: true` in every per-PR compose file)\n  previews  docker compose on this host\n\nnext:\n  1. UI          https://control.preview.example.com\n     DNS must already answer for preview.example.com and *.preview.example.com; the wildcard certificate is\n     requested on first request and can take a minute (docs/bootstrap.md §10 if it never arrives).\n  2. Health      curl -sf https://api.preview.example.com/api/health\n  3. Submit      curl -X PUT https://api.preview.example.com/api/deployments/<id> \\\n                   -H \"Authorization: Bearer $PSTACK_TOKEN\" \\\n                   -H 'content-type: application/json' \\\n                   -d '{\"spec\": \"<preview.yml>\", \"compose\": \"<docker-compose.yml>\"}'\n                 then POST /api/deployments/<id>/up. A spec with `stack: pr-${PR}` needs\n                 its variables on every call (…?PR=7) — and the SAME ones on `down`, or\n                 teardown targets a different stack than deploy created. Full route list:\n                 the header of src/api.ts. Or just use the UI.\n  4. Upgrade     pstack upgrade  — reads this .env so the token, domain and challenge\n                 mode all survive. Never from inside the control stack; SSH in.\n  5. Rotate      PSTACK_TOKEN=<new> pstack init …  — re-running rewrites .env and recreates\n                 the container. Omit it entirely to be handed a fresh one. The only copy\n                 lives in <DATA>/control/.env (0600); the DNS credential is a\n                 separate secret in dns.env and is not affected.\n\n  PSTACK_TOKEN: taken from the environment (stored 0600 in <DATA>/control/.env)\n\n",
  "stderr": ""
}
